"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.resolvePromisedProperties = resolvePromisedProperties;
async function resolvePromisedProperties(maybePromised) {
    const promised = await maybePromised;
    const entries = Object.entries(promised);
    const resolvedEntries = await Promise.all(entries.map(async ([key, value]) => [key, await value]));
    return Object.fromEntries(resolvedEntries);
}
//# sourceMappingURL=resolvePromisedProperties.js.map