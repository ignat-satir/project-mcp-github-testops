"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.sourceCode = sourceCode;
function sourceCode(user) {
    const factories = {};
    const options = {};
    if (user.plugins) {
        for (const [key, value] of Object.entries(user.plugins)) {
            if (typeof value === 'function') {
                factories[key] = value;
            }
            else if (Array.isArray(value)) {
                factories[key] = value[0];
                options[key] = value[1];
            }
            else {
                options[key] = value;
            }
        }
    }
    return {
        enabled: user.enabled ?? true,
        factories,
        options,
        plugins: [],
    };
}
//# sourceMappingURL=sourceCode.js.map